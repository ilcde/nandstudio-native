# Xor, search, Android header and public guides

Commits 1ea65a7 and 7d33b89 implement explicit visible-buffer Reload & Eval,
preserve the last valid chip on load errors, expose dependency diagnostics,
warn about empty local starters, repair active-editor find/replace, move it
under More, and inset the Android header. Two reported-Xor cases match Java
output bytes. All 65 broader differential cases pass; the baseline audit finds
881 unchanged files. Windows: 2012 core, 2144 hardware, 244 GUI checks pass.

Commit 9452ec3 adds platform installation/build/use/course coverage guides,
contribution instructions and offline desktop documentation installation. CI
run 36104136260 passes all six builds, packaged desktop GUI tests, sanitizers
and the exact-APK Android toolbar check. Its API 36 x86-64 emulator reports top
inset 41 and all four toolbar button origins at y=49. Both Android ABIs pass
ELF/ZIP 16 KB checks; runtime testing uses 4 KB pages. Full workflows remain blocked.

The initial Android test had a compile error involving a typed JNI context,
corrected in 7d33b89. The following runtime run failed to produce a report;
complete startup diagnostics were added. The same earlier APK then passed
locally, and the next CI APK passed its emulator run. The first runtime timeout's
cause remains unproven; no failed check was bypassed for release publication.

Local emulator visual artifacts were discovered separately and recorded as
`android.emulator-rendering`, with screenshot evidence. Emulator 36.3.10 using
SwiftShader showed triangular backgrounds/buttons. Disabling emulator Vulkan
did not resolve this, and forcing Qt software rendering produced a blank surface
with EGL errors. No renderer override ships. Geometry success does not certify
visual correctness. Physical ARM64 rendering remains unverified.

The current Windows package is `dist/NandStudio-guides-windows-x86_64.zip` locally.
Public downloads and immutable provenance are attached to the GitHub development
release; `platforms.json` records publication and platform-stage status.
Core OS fallback, Java extension binaries, Android SAF/lifecycle and remaining
legacy operations still block complete migration and all twelve project workflows.
